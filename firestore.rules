rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isAdmin() {
      return request.auth != null
        && request.auth.uid == "KSXJJqnu3FhuFcTye2lRlxxct6r2";
    }

    match /users/{userId} {
      allow read: if isAdmin()
        || (
          request.auth != null
          && request.auth.uid == userId
        );

      allow create: if request.auth != null
        && request.auth.uid == userId;

      allow update: if isAdmin()
        || (
          request.auth != null
          && request.auth.uid == userId
          && request.resource.data.balance == resource.data.balance
        );

      allow delete: if false;
    }

    match /walletTransactions/{transactionId} {
      allow read: if isAdmin()
        || (
          request.auth != null
          && resource.data.uid == request.auth.uid
        );

      allow write: if false;
    }

    match /orders/{orderId} {
      allow read: if isAdmin()
        || (
          request.auth != null
          && resource.data.uid == request.auth.uid
        );

      allow write: if false;
    }

    match /notifications/{notificationId} {
      allow read: if isAdmin()
        || (
          request.auth != null
          && resource.data.uid == request.auth.uid
        );

      allow write: if false;
    }

    match /platformTransactions/{transactionId} {
      allow read: if isAdmin();
      allow write: if false;
    }


    // Marketplace collections are accessed through server-side APIs.
    // Clients must not directly create, edit, or delete marketplace data.

    match /marketplaceProducts/{productId} {
      allow read: if request.auth != null;
      allow write: if false;
    }

    match /marketplaceInventory/{inventoryId} {
      allow read: if isAdmin();
      allow write: if false;
    }

    match /marketplaceOrders/{orderId} {
      allow read: if isAdmin()
        || (
          request.auth != null
          && resource.data.uid == request.auth.uid
        );
      allow write: if false;
    }

    match /marketplaceTransactions/{transactionId} {
      allow read: if isAdmin()
        || (
          request.auth != null
          && resource.data.uid == request.auth.uid
        );
      allow write: if false;
    }

    match /marketplaceLogs/{logId} {
      allow read: if isAdmin();
      allow write: if false;
    }
  }
}
